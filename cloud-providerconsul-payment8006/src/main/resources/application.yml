server:
  port: 8006

spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      host: 47.114.45.155
      port: 8500
      discovery:
        service-name: ${spring.application.name}